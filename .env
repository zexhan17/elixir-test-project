# JWT signing secret (overrides SECRET_KEY_BASE for Joken signer)
JOKEN_SIGNING_SECRET=4wPq8v9Zx3sN7bRkY6hF2mU0cTqV1nA5yLzQeH8jR0pSxC6v

# Phoenix / runtime
# When running releases you may need to set PHX_SERVER to true
PHX_SERVER=true

# HTTP port for dev/prod
PORT=4000

# Allowed CORS ORIGINS
origins=http://localhost:5173,http://localhost:5174

# Host for URL generation in prod
PHX_HOST=localhost

# Database (runtime.exs expects a DATABASE_PATH in prod)
DATABASE_PATH=/elixir_test_project_dev.db
# Optional pool size
POOL_SIZE=5

# Secrets
# SECRET_KEY_BASE is used to sign/encrypt cookies and other secrets
SECRET_KEY_BASE=9f2a8b3d6c7e4f1a0b9c8d7e6f5a4b3c2d1e0f9a8b7c6d5e4f3a2b1c0d9e8f7

# Optional DNS cluster query used by the app (if used)
DNS_CLUSTER_QUERY=

# Release awareness used in Application (used to skip migrations)
RELEASE_NAME=dev

# Mailer (examples, commented out by default)
# MAILGUN_API_KEY=your_mailgun_api_key
# MAILGUN_DOMAIN=your_mailgun_domain

# SSL paths (optional, only if enabling https in runtime.exs)
# SOME_APP_SSL_KEY_PATH=/path/to/ssl/key.pem
# SOME_APP_SSL_CERT_PATH=/path/to/ssl/cert.pem

# Notes:
# - Replace the placeholder secrets with secure values before deploying to production.
# - You can generate a Phoenix secret with `mix phx.gen.secret` and place the value in SECRET_KEY_BASE.